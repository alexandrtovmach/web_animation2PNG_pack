module.exports = {
  link: 'http://www.chartjs.org/samples/latest/charts/doughnut.html', // link to the page
  recordDuration: 0.2, // duration of recording in second (default 1)
  fullPage: false, // recodrding full page
  FPS: 60, // custom FPS. high FPS === (high quality & large animation); low FPS === (low quality & weightless animation);  Optimal and default is 60
}